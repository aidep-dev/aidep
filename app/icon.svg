<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 4 4">
  <!-- aidep: the dying mark, full bleed. The block in the dying amber; the
       bite is transparent, so it takes the tab bar's own color and the
       silhouette reads on light and dark chrome alike. 4x4 grid = crisp 4px
       cells at 16px. -->
  <path d="M0 0H2V1H3V2H4V4H0Z" fill="#e0ae57"/>
</svg>
